clear
clc
x = [4, -9, 0, 16, -1, 25];
y = zeros(size(x)); 
for i = 1:length(x)
    if x(i) > 0
        y(i) = sqrt(x(i));     
    elseif x(i) == 0
        y(i) = 0;              
    else
        y(i) = abs(x(i));       
    end
end
disp(y)
